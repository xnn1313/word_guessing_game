<view class="page-shell campaign-page">
  <view class="campaign-header">
    <view>
      <view class="page-kicker">CAMPAIGN · 挑战路线</view>
      <view class="page-title">分类闯关</view>
      <view class="page-subtitle">完成当前关，沿着难度路线继续前进。</view>
    </view>
  </view>

  <view class="campaign-progress">
    <view class="progress-copy">
      <view><text>{{totalStars}}</text> / {{maxStars}} 星</view>
      <text>13 个分类 · 260 个关卡</text>
    </view>
    <view class="progress-track">
      <view class="progress-fill" style="width: {{maxStars ? totalStars / maxStars * 100 : 0}}%"></view>
    </view>
  </view>

  <view class="loading-state" wx:if="{{loading && !categories.length}}">正在读取关卡…</view>

  <view class="category-list" wx:else>
    <view class="category" wx:for="{{categories}}" wx:key="id" wx:for-item="category">
      <view class="category-header {{category.expanded ? 'expanded' : ''}}" data-id="{{category.id}}" bindtap="toggleCategory" hover-class="card-pressed">
        <view class="category-emoji">{{category.emoji}}</view>
        <view class="category-copy">
          <text class="category-name">{{category.name}}</text>
          <text class="category-description">{{category.description}}</text>
          <view class="category-track">
            <view style="width: {{category.max_stars ? category.stars / category.max_stars * 100 : 0}}%"></view>
          </view>
        </view>
        <view class="category-right">
          <text class="category-stars">{{category.progressText}}</text>
          <text class="category-chevron {{category.expanded ? 'open' : ''}}">⌄</text>
        </view>
      </view>

      <view class="level-area" wx:if="{{category.expanded}}">
        <view class="difficulty-line">
          <text>入门</text>
          <view class="difficulty-dots"><view></view><view></view><view></view><view></view><view></view></view>
          <text>高手</text>
        </view>
        <view class="level-grid">
          <view
            class="level-button difficulty-{{level.difficulty}} {{level.unlocked ? '' : 'locked'}}"
            wx:for="{{category.levels}}"
            wx:key="id"
            wx:for-item="level"
            data-id="{{level.id}}"
            data-unlocked="{{level.unlocked}}"
            bindtap="openLevel"
            hover-class="level-pressed"
          >
            <text class="level-number">{{level.order}}</text>
            <text class="level-label">{{level.difficulty_label}}</text>
            <text class="level-stars">{{level.unlocked ? level.starText : '未解锁'}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
